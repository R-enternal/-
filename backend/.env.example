# 应用配置
APP_NAME=仓脉智诊
DEBUG=True
HOST=0.0.0.0
PORT=9901

# MySQL 配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=060311
MYSQL_DB=cangweiyun

# Redis 配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# 安全配置
JWT_SECRET_KEY=change-me-in-production
JWT_EXPIRE_MINUTES=1440
AUTH_ENABLED=False

# 模拟数据配置
SIMULATE_INTERVAL_SECONDS=60

# 监测配置（阈值初值，按设备类型模板可在数据库中覆盖）
ALERT_DEBOUNCE_COUNT=3

# LLM 配置（OpenAI 兼容接口；留空则 Agent 走内置规则降级，可离线演示）
# LLM_API_KEY=sk-xxx
# LLM_BASE_URL=https://api.deepseek.com/v1
# LLM_MODEL=deepseek-chat

# Embedding 配置（RAG 知识库向量化，OpenAI 兼容接口）
# EMBEDDING_API_KEY=xxx
# EMBEDDING_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
# EMBEDDING_MODEL=embedding-2

# SQL 日志（调试 SQL 时开启）
SQL_ECHO=False
