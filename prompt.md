# 仓维云 · Codex 分步指令（第 3 阶段）

> 使用方法：先把「全局背景」发给 codex，然后按顺序逐条发送指令。
> 每步 codex 交付后由 Claude 按审查流程验收，通过后再发下一步。
> 第 1~9 步（设备域 + 监测域）已完成并验证，无需重发。

---

## 全局背景（第一步之前先发这个）

```
背景：仓维云后端已完成设备域和监测域（仓库/设备/点位CRUD、阈值模板、
模拟数据、阈值/趋势告警、健康度、APScheduler定时任务），全部表结构已建好
（work_order/work_order_part/spare_part/stock_record/sys_user/notification
表已存在，无需新建迁移）。

全局要求（每个任务都要遵守）：
1. 分层不变：api只做参数校验和响应包装，业务逻辑在services，算法在core；
2. 响应格式统一用 schemas/common.py 的 ok()，业务失败抛 HTTPException；
3. 完成后必须：python -m ruff check app scripts 和 python -m mypy app 全过，
   且现有 scripts/test_*.py 全部回归通过；
4. 每个新功能提供对应的 scripts/test_xxx.py 验收脚本（可重复运行）。
```

---

## 第 10 步：补告警处理接口

```
在现有告警 API（列表/详情/手动检查已存在）基础上补充：
1. 确认告警接口：状态 PENDING → HANDLED，记录 handled_by/handled_at/handle_note；
2. 忽略告警接口：状态 PENDING → IGNORED；
3. 已处理/已忽略的告警不允许重复处理（返回400）。

验收：
- 能将告警改为已确认或已忽略，字段记录完整；
- 重复处理被拒绝；
- 确认告警后重新计算健康度，设备评分应回升（这是闭环亮点，请验证）。
```

---

## 第 11 步：实现工单基础流程

```
实现工单核心流程（work_order表已存在）：
1. 创建工单：order_no 自动生成（如 WO+时间戳+序号），必填仓库/设备/标题；
2. 状态流转接口：PENDING_ASSIGN → PENDING_EXECUTE → IN_PROGRESS
   → PENDING_ACCEPT → COMPLETED，支持任意状态 → CANCELLED；
3. 每次流转校验前置状态，非法流转返回400（如未派单不能执行）；
4. 列表/详情接口，支持按状态/设备/指派人过滤。

验收：
- 完整走一遍 创建→派单→执行→验收→完成；
- 非法跳转（如创建后直接验收）被拒绝；
- 提供 scripts/test_work_order.py 验收脚本。
```

---

## 第 12 步：实现告警转工单

```
实现告警一键转工单：
1. 对已确认（HANDLED）的告警提供"转工单"接口；
2. 生成的工单 source=ALERT、alert_id 关联告警，标题/描述从告警带出；
3. 转工单后告警状态改为 CONVERTED，且不允许重复转；
4. 工单详情中能看到来源告警信息。

验收：
- 告警→工单→告警状态变 CONVERTED 全链路可验证；
- 重复转工单被拒绝；
- PENDING 状态的告警不允许直接转（必须先确认）。
```

---

## 第 13 步：实现备件与库存

```
实现备件管理模块（spare_part/stock_record表已存在）：
1. 备件 CRUD，part_code 唯一；
2. 入库/出库接口：库存增减与流水记录必须在同一事务提交，
   出库时库存不足要拒绝（400）；
3. 流水记录包含变动数量、变动后余量、操作人、备注；
4. 库存查询接口：支持按仓库过滤，低于安全库存的备件能筛出来。

验收：
- 入库→出库→流水和库存一致；
- 超量出库被拒绝且库存不变（事务回滚）；
- 提供 scripts/test_spare_part.py。
```

---

## 第 14 步：实现工单消耗备件

```
实现工单消耗备件：
1. 工单执行中可以登记使用备件（写 work_order_part 明细）；
2. 工单完成（COMPLETED）时自动扣减库存 + 生成 WORK_ORDER 类型流水，
   与工单状态流转在同一事务；
3. 库存不足时工单不能完成，返回明确错误；
4. 工单取消时已登记的备件消耗要回滚（如果还没完成则直接删除明细即可）。

验收：
- 工单完成后库存减少、流水可查、明细可查，三者一致；
- 库存不足时完成工单被拒绝；
- 提供对应验收脚本。
```

---

## 第 15 步：实现认证（带开关）

> 提示：本步也可挪到第 18 步之后执行，联调期完全不受 401 干扰。

```
实现认证模块（sys_user表已存在，jwt配置已在config）：
1. passlib bcrypt 密码哈希，/api/auth/login 返回 JWT；
2. get_current_user 依赖注入，校验 token 并注入当前用户；
3. 在 config 加 auth_enabled 开关：开发联调期 False（所有接口放行），
   演示前打开；打开后除 /health 和 /api/auth/login 外全部需要 token；
4. 提供 scripts/init_users.py：初始化 admin/维修工/观察者三个演示账号；
5. 现有测试脚本在 auth_enabled=False 下必须全部照常通过。

验收：
- 登录拿 token → 带 token 访问正常 → 不带 token 401；
- 开关关闭时所有接口免认证；
- 角色字段写入 token，为后续权限做准备（本步只认证不鉴权）。
```

---

## 第 16 步：实现通知中心

```
实现通知中心（notification表已存在）：
1. 三个触发点自动产生通知：
   - 告警生成时（alert_service 创建告警处）→ ALERT 类型；
   - 备件库存低于安全库存时（出入库后检查）→ STOCK 类型；
   - 工单派单时（指派给某人）→ WORK_ORDER 类型；
2. 通知发给谁：MVP 阶段发给所有 ADMIN 用户 + 相关指派人即可；
3. 接口：通知列表（区分已读未读）、未读数、标记已读（单条/全部）；
4. 通知产生逻辑写在对应 service 里，不要污染 core 层。

验收：
- 触发告警/低库存/派单后通知表有新记录；
- 已读/未读/标记接口正常；
- 提供 scripts/test_notification.py。
```

---

## 第 17 步：搭前端骨架

```
搭建前端（frontend/ 目录，Vue3 + Vite + Element Plus + Pinia + Vue Router）：
1. axios 封装：baseURL 指向后端 9901，响应拦截器统一拆 {code,message,data}，
   code 非 200 时 ElMessage 报错；预留 token 请求头注入；
2. 登录页 + 主布局（侧边栏 + 顶栏）；
3. 五个页面骨架：设备管理、监测看板、告警中心、工单管理、备件库存；
4. vite proxy 代理 /api 到 localhost:9901。

验收：
- npm run dev 能启动，五个路由可访问；
- 至少设备列表页真正调通后端接口显示数据。
```

---

## 第 18 步：前后端联调

```
完成前后端联调，五个页面全部接上真实接口：
1. 设备页：设备列表/新建（含自动点位展示）/点位阈值编辑；
2. 监测看板：设备健康度卡片 + 健康度趋势图（用 /api/health 数据）；
3. 告警中心：告警列表 + 确认/忽略/转工单操作；
4. 工单页：列表 + 状态流转操作；
5. 备件页：列表 + 入库/出库。

验收：
- 一条完整业务流在前端跑通：模拟数据 → 产生告警 → 确认告警
  → 转工单 → 派单执行 → 完成；
- 字段不匹配问题全部解决，页面无 console 报错。
```

---

## 第 19 步：测试与演示收尾

```
收尾（为演示做准备）：
1. 把 scripts/test_*.py 改造为 pytest 风格（断言失败即红），
   保留现有的"纯函数单测+集成测试"结构；
2. 提供 scripts/seed_demo.py：重置数据库 → 灌阈值模板 → 建演示仓库/
   设备/备件/用户 → 补 2 小时模拟数据 → 触发几条告警和健康度记录，
   一条命令出完整演示环境；
3. 提供一键启动脚本（后端+前端）和 README：环境要求、启动步骤、
   演示账号、演示流程解说词；
4. 打开 auth_enabled 验证认证全链路。

验收：
- pytest 全绿；
- seed_demo.py 一条命令跑完，登录后五个页面都有数据；
- README 能让陌生人在 10 分钟内跑起项目。
```
